name = "ignite-growth-uae"
# Used by `wrangler pages publish` to locate the static build output
pages_build_output_dir = "dist"

# Cloudflare Pages Functions configuration
compatibility_date = "2024-01-01"
